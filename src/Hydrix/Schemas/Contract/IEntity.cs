namespace Hydrix.Schemas.Contract
{
    /// <summary>
    /// Represents a base interface for all entities in the system.
    /// </summary>
    /// <remarks>Implementing this interface allows for consistent handling of entity types across the
    /// application. It serves as a marker interface, providing a common type for all entities without imposing any
    /// specific behavior or properties.</remarks>
    public interface IEntity
    { }
}
